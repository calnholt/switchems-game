import { CardInfoTooltipDirective } from "./card-info-tooltip.directive";

describe('TooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new CardInfoTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
