import { ConvertEmbeddedTextPipe } from './convert-embedded-text.pipe';

describe('ConvertEmbeddedTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertEmbeddedTextPipe();
    expect(pipe).toBeTruthy();
  });
});
