import { SimpleTooltipPipe } from './simple-tooltip.pipe';

describe('SimpleTooltipPipe', () => {
  it('create an instance', () => {
    const pipe = new SimpleTooltipPipe();
    expect(pipe).toBeTruthy();
  });
});
